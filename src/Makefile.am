
CFLAGS = -O1
LDFLAGS =

# this lists the binaries to produce, the (non-PHONY, binary) targets in
# the previous manual Makefile
bin_PROGRAMS = ma
ma_SOURCES = ma.c cma.h
shell_LDADD   = libcma.la

	

lib_LTLIBRARIES     = libcma.la
libcma_la_SOURCES = cma.c cma.h



